{
  "name": "@obikai/db",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "//files": "Ship dist + the migrate-mongo migrations so the api image's migration runner can resolve them at runtime (apps/api/src/cli/migrate.ts).",
  "files": ["dist", "migrations"],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run"
  },
  "dependencies": {
    "@obikai/config": "workspace:*",
    "@obikai/domain": "workspace:*",
    "@obikai/gdpr": "workspace:*",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "mongodb-memory-server": "^10.1.2",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
