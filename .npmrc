# Strict, reproducible installs (ADR-0001). No phantom dependencies: a package can only
# import what it explicitly declares — this is what protects the import boundaries.
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
prefer-workspace-packages=true
link-workspace-packages=true
